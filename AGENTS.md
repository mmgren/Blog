## 开发

启动本地预览时使用：

```bash
npm run dev
```

站点根路径是 `/Blog/`，请从 `http://localhost:4321/Blog/` 打开。

## 内容

- 文章写在仓库根目录的 `posts/`，不要放到 `src/pages/`。
- 以下划线开头的 Markdown（例如 `_template.md`）不会被发布。
- `draft: true` 的文章只在开发模式可见。
